// Portfolio state lands here in ENG-69, the plan model in ENG-71. Money, quantity, rate, and
// timestamp columns come from `../columns.ts` — a schema-guard test fails the build otherwise.
export {};

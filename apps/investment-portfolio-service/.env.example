# Compose and `node --env-file` both read .env from this directory on their own.

POSTGRES_DB=evergreen
POSTGRES_USER=evergreen
POSTGRES_PASSWORD=change-me

# Must match the port in DATABASE_URL.
POSTGRES_PORT=5433

DATABASE_URL=postgres://evergreen:change-me@localhost:5433/evergreen
PORT=4000
